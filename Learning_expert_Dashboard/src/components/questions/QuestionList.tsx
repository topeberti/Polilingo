import { Box, Typography } from '@mui/material';

export const QuestionList = () => {
    return (
        <Box>
            <Typography variant="body1">Question List component placeholder</Typography>
        </Box>
    );
};
