import { Box, Typography } from '@mui/material';

export const QuestionEditor = () => {
    return (
        <Box>
            <Typography variant="body1">Question Editor component - use the resource form instead</Typography>
        </Box>
    );
};
